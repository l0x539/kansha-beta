import CookiesConsent from "../CookiesConsent";
import MainCanvas from "../MainCanvas";

const HomePage = () => {
  return (<>
    <CookiesConsent />
    <MainCanvas />
  </>);
};

export default HomePage;